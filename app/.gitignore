node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Tauri
/src-tauri/target/

# Semantic search: bundled embedding model (run scripts/fetch-model.sh) and
# generated indices (run `cargo run -p build-index --release`). Both are large
# and reproducible, so they stay out of git; SOURCE.md in each dir is kept.
/src-tauri/resources/model/*.onnx
/src-tauri/resources/model/*.onnx_data
/src-tauri/resources/model/*.json
/src-tauri/resources/search-index/*.idx
/src-tauri/.fastembed_cache/

# Claude Code local config
.claude/
